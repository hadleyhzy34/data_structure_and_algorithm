#include <iostream>
#include <string.h>
#include <vector>

typedef std::vector<std::vector<int>> pairArray;
int eraseOverlapIntervals(pairArray &intervals) {
  int n = intervals.size();
  // number of
  int dp[n];
}
