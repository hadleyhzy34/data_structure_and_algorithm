#include <iostream>
#include <unordered_map>
#include <vector>

int numberOfArithmeticSlices(std::vector<int> &nums) {
  int n = nums.size();
  std::unordered_map<int, int> umap;
  for (auto i = 0; i < n; i++) {
  }
}
